## Contributers
- CoolCat467

## Purpose
This project serves as a light review of some basic data structure
concepts.

## Resources
In problem 1, `__setitem__`, `_setitem_slice`, `__getitem__`,
`_getitem_slice`, `__delitem__`, and `_delitem_slice` all copied but
modified to fit this particular usecase from an older project I haven't
published.

In problem 2, stole `SupportsLe` protocol idea from the same project,
but this time I did it way simpler than that time so I don't really know
if I would count it as a reference or not but I'm adding it to be safe.
